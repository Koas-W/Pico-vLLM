# Core runtime requirements from README.md.
# This H200 environment has a CUDA 12.8 driver. Pin PyTorch to the matching
# CUDA 12.8 wheel; the default PyPI torch wheel may require a newer driver.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
triton==3.6.0
transformers>=4.0
safetensors>=0.4

# Useful for the repository's test suite and profiling/benchmark scripts.
pytest>=7.0
numpy>=1.24
matplotlib>=3.7
